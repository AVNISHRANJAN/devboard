[extend]
useDefault = true

[allowlist]
description = "Known non-secret examples and generated dependency files"
paths = [
  '''(^|/)\.env\.example$''',
  '''(^|/)package-lock\.json$''',
  '''(^|/)go\.sum$'''
]
